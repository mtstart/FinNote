

export namespace ButtonLabelSpec {
    export type AsObject = {
        key: string,
        displayName: string,
        ref?: string,
        order: number,
        description: string,
        shortcutKey: string,
        color: string,
        icon?: string,
        // itemCount: number,
    }
}