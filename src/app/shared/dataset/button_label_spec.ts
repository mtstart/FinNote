

export namespace ButtonLabelSpec {
    export type AsObject = {
        key: string,
        displayName: string,
        color: string,
        order: number,
        description: string,
        shortcutKey: string,
        // itemCount: number,
    }
}